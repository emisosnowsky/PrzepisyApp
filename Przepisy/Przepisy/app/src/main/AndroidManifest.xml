<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emilia.przepisy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GlownyActivity"
            android:label="Przepisy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ZalogujActivity"
            android:label="Przepisy" />
        <activity
            android:name=".ZarejestrujActivity"
            android:label="Przepisy" />
        <activity
            android:name=".WybierzPrzepisyActivity"
            android:label="Przepisy" />
        <activity
            android:name=".PrzepisyActivity"
            android:label="Przepisy" />
        <activity android:name=".WybranyPrzepisActivity"></activity>
    </application>

</manifest>